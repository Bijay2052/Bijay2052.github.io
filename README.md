# Bijay Pandey — personal website

A one-page portfolio connecting cloud reliability engineering and Physical AI research. The site is a static `index.html` file with no build step.

To publish it at the account homepage, name this repository `Bijay2052.github.io` and set **Settings → Pages → Build and deployment** to **Deploy from a branch**, `main`, `/ (root)`.
